# Release v0.1.0 (Draft)

Release date: 2025-10-02

Changelog
---------
Included changes (see full changelog in CHANGELOG.md):

- Initial PyPI release as `equation-phase-portrait-tool`.
- Single-source version at `equation_phase_portrait_tool/_version.py`.
- Top-level import package `equation_phase_portrait_tool` exposing `__version__` and `backend`.
- Module CLI entry (`__main__.py`) and compatibility shim (`main.py`) for flexible invocation.
- Console scripts: `eqpp-server` (existing) and `equation-phase-portrait-tool`.
- Packaging configuration updates in `pyproject.toml` (console scripts, package discovery, dev extras).
- Included static assets under `backend/static` and excluded development artefacts (`backend/jobs.sqlite`, `backend/worker_runs.log`).
- CI workflow `.github/workflows/release.yml` for build/test and tag-triggered publishing to TestPyPI/PyPI.
- Release documentation: `RELEASE.md` and README release section.
- Dev/test dependency adjustments (added `requests` to dev requirements to support integration tests).

Verification performed
----------------------
- Local tests: all tests passed (4 passed).
- Built sdist and wheel locally and installed the wheel successfully.
- Verified CLI via `eqpp-server --help` and `equation-phase-portrait-tool --help`.
- Published to TestPyPI and PyPI.

Maintainer instructions (for publishing GitHub release)
------------------------------------------------------
1. Confirm tag exists and points to the release commit:
   - git tag -l | grep v0.1.0
   - git show v0.1.0

2. On GitHub create a new release (Draft) for tag `v0.1.0` or use this draft:
   - Title: v0.1.0
   - Body: copy the “Changelog” section above or paste the contents of [`CHANGELOG.md:1`](CHANGELOG.md:1)
   - Attach build artifacts if desired (optional): upload the sdist/wheel from the CI artifacts or local dist/*
   - Mark the release as a Draft until you are ready to publish publicly.

3. Publish steps and verification:
   - If you used CI for publishing, TestPyPI and PyPI uploads should have occurred automatically when the tag was pushed (see Actions run for `release.yml`).
   - Manually verify the package on PyPI/TestPyPI:
     - Check the package page and download links.
     - Optionally install from TestPyPI in a clean venv to verify runtime.

4. Post-release housekeeping:
   - Attach the changelog/release notes to the GitHub release body.
   - Close the release milestone and update the project board/issues.
   - Announce the release to any channels you use.

References
----------
- CHANGELOG: [`CHANGELOG.md:1`](CHANGELOG.md:1)
- Release procedure: [`RELEASE.md:1`](RELEASE.md:1)
- CI workflow: [`.github/workflows/release.yml:1`](.github/workflows/release.yml:1)

Maintainer contact: uliarp15@gmail.com